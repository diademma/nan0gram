# Nan0gram Messenger UI & Android WebView Bridge — Технический путеводитель

Данный проект содержит интерфейс мессенджера React, каскадные модули стилей и нативный системный слой взаимодействия с ОС Android (Kotlin) через WebView [1].

---

## 1. Структура директорий проекта

Модули распределены по двум изолированным папкам в корне активов WebView, а нативный код разбит на базовое ядро, модули системного интерфейса в подпапке `bridge/` и изолированные компоненты интерфейса в подпапке `ui/` [1]:

    📁 app/src/main/assets/
    │
    ├── 📁 n0g_messenger_ui/              # [Папка 1] Фронтенд-интерфейс чата на React и стили (ESM & CSS) [1]
    │   ├── 📄 n0g_msg_utils_101.js
    │   ├── 📄 n0g_msg_bridge_102.js
    │   ├── 📄 n0g_msg_settings_view_103.js
    │   ├── 📄 n0g_msg_ui_modals_104.js
    │   ├── 📄 n0g_msg_chat_area_105.js
    │   ├── 📄 n0g_msg_app_entry_106.js
    │   ├── 📄 style-base.css             # Базовые переменные и сброс (Часть 1 из 6)
    │   ├── 📄 style-sidebar.css          # Сайдбар и шапка чата (Часть 2 из 6)
    │   ├── 📄 style-messages.css         # Сообщения и поле ввода (Часть 3 из 6)
    │   ├── 📄 style-modals.css           # Модалки (меню, профиль, лайтбокс, настройки) (Часть 4 из 6)
    │   ├── 📄 style-media.css            # Медиа (альбомы, видео, адаптив, глитч, гласс) (Часть 5 из 6)
    │   └── 📄 style-player.css           # Аудиоплеер, запись голоса, файловые сообщения (Часть 6 из 6)
    │
    ├── 📁 n0g_bridge/                    # [Папка 2] Системный мост связи с Kotlin (Global Scripts)
    │   ├── 📄 bridge-recorder.js
    │   ├── 📄 bridge-core.js
    │   └── 📄 bridge-player.js
    │
    ├── 📄 cipher.js                      # Библиотека шифрования (Global Script)
    ├── 📄 react-lib.js                   # Ядро библиотеки React (Global Script)
    └── 📄 index.html                     # Точка входа для WebView

    📁 app/src/main/java/com/example/     # [Папка 3] Нативные модули ядра (Kotlin)
    │
    ├── 📄 MainActivity.kt                # Точка старта приложения, запуск Compose-окружения
    ├── 📄 AppDatabase.kt                 # База данных приложения (Room/SQL) и репозиторий
    ├── 📄 BridgeEngine.kt                # Фоновые JS-эффекты и нативные симуляторы кликов
    ├── 📄 BridgeScripts.kt               # Константы инжектируемых JS-скриптов и селекторы
    ├── 📄 MediaManager.kt                # Менеджер сохранения и загрузки локальных медиафайлов
    ├── 📄 UpdateChecker.kt               # Модуль проверки обновлений APK и фонового развертывания OTA
    ├── 📄 UpdateDialog.kt                # Диалоговые окна инсталлятора нативных обновлений
    │
    ├── 📁 bridge/                        # [Подпапка] Модули системного JS-интерфейса (пакет com.example)
    │   ├── 📄 UkrnetJsInterface.kt       # Системный интерфейс управления скрытой почтой Ukr.net
    │   ├── 📄 MessengerJsInterface.kt    # Облегченный JS-мост React-мессенджера (делегат хелперов)
    │   ├── 📄 MessengerCryptoHelper.kt   # Хелпер нативной криптографии (AES-GCM / RSA)
    │   ├── 📄 MessengerDownloadHelper.kt # Хелпер загрузки медиафайлов в память устройства
    │   ├── 📄 MessengerComposeHelper.kt  # Хелпер управления формой и загрузкой файлов на почту
    │   ├── 📄 MessengerDbHelper.kt       # Хелпер взаимодействия с БД и кэшем из JS-слоя
    │   └── 📄 MessengerAudioHelper.kt    # Хелпер управления аудиофокусом Android
    │
    └── 📁 ui/                            # [Подпапка] Экраны и элементы интерфейса (пакет com.example)
        ├── 📁 theme/                     # Конфигурация темы Compose (шрифты, цвета)
        ├── 📄 AppScreen.kt               # Контейнер экрана, связка БД, медиа-менеджера и слоев UI
        ├── 📄 WebViewLayer.kt            # Слой управления WebViews, стейтами и системными лаунчерами
        ├── 📄 UkrnetWebView.kt           # Конструктор и обработчики скрытого WebView почты Ukr.net
        ├── 📄 MessengerWebView.kt        # Конструктор и обработчики WebView React-мессенджера
        ├── 📄 MediaLimitsProcessor.kt    # Валидатор ограничений медиафайлов и вывода JS-оповещений
        ├── 📄 MediaUtils.kt              # Утилиты копирования, кодирования Base64 и шифрования AES-GCM
        ├── 📄 WebViewHelpers.kt          # Вспомогательные методы контроля URL-адресов почтового шлюза
        ├── 📄 FallbackAssetsPathHandler.kt # Обработчик перехвата локальных ресурсов мессенджера (OTA)
        └── 📄 LogPanel.kt                # Панель разработчика (вывод логов, слайдер прозрачности)

---

## 2. Схема загрузки скриптов и подчинения

### 2.1 Схема подключения JS-скриптов

Скрипты в `index.html` должны загружаться в строгой последовательности для сохранения цепочки глобальных зависимостей:

1. **`react-lib.js`** — предоставляет глобальное ядро React (`window.React`).
2. **`cipher.js`** — предоставляет методы шифрования (`window.nanoCipher`).
3. **`n0g_bridge/bridge-recorder.js`** — независимый глобальный обработчик жестов записи звука.
4. **`n0g_bridge/bridge-core.js`** — ядро системного моста (требует `cipher.js`, регистрирует `window.nan0gram`).
5. **`n0g_bridge/bridge-player.js`** — MutationObserver для аудиоплеера (активируется после загрузки DOM).
6. **`n0g_messenger_ui/n0g_msg_app_entry_106.js`** — точка входа React-интерфейса (ES-модуль, импортирует модули `101`–`105`, связывается с мостом `102` и вешает реакцию на события из `bridge-core`).

### 2.2 Схема подключения каскадных стилей (CSS)

Стили в теге `<head>` файла `index.html` должны подключаться в строгой последовательности для корректной работы каскадности и переопределения специфичности правил:

1. **`n0g_messenger_ui/style-base.css`** — базовые переменные, системные цвета и сброс.
2. **`n0g_messenger_ui/style-sidebar.css`** — разметка сайдбара и шапки чата.
3. **`n0g_messenger_ui/style-messages.css`** — баблы сообщений, закрепы и область ввода.
4. **`n0g_messenger_ui/style-modals.css`** — настройки, профиль, лайтбокс и контекстные меню.
5. **`n0g_messenger_ui/style-media.css`** — брейкпоинты адаптивности, сетки медиа-альбомов, глитч-эффекты и глассморфизм.
6. **`n0g_messenger_ui/style-player.css`** — аудиоплеер, Scarlet-кнопка отмены, индикатор-замочек и стили файлов.

---

## 3. Описание модулей нативного моста (Папка `n0g_bridge/`)

### 📄 bridge-recorder.js
* **Тип:** Чистый JS (ES6). Глобальный контекст.
* **Ответственность:** Интерфейс и оверлей записи голоса. Обработка длинного удержания (long-press) кнопки микрофона, анимации REC-бара с таймером, кнопки отмены записи, защита от случайных кликов и перехват аудиофокуса нативного микрофона Android.
* **Внутренние зависимости:** Нет. Полностью автономен.

### 📄 bridge-core.js
* **Тип:** Чистый JS (ES6). Глобальный контекст.
* **Ответственность:** Ядро моста и криптография. Настройка фонового Stealth Composer (поиск активного инпута на странице, генерация сессионных AES/RSA ключей, шифрование текста и авто-отправка). Предоставляет глобальный объект `window.nan0gram`. Слушает события от Kotlin: `email-received` (с дешифрацией AES/RSA конвертов и авто-кэшем), `local-media-sent` и др.
* **Место расширения:** Содержит изолированные методы `_buildBody()` и `submitMedia()`, куда встроится будущая стратегия быстрого отключения шифрования и простой обфускации метаданных.
* **Внутренние зависимости:** Требует предварительной загрузки `cipher.js` в глобальную область видимости.

### 📄 bridge-player.js
* **Тип:** Чистый JS (ES6). Глобальный контекст.
* **Ответственность:** Кастомный Telegram-подобный плеер голосовых сообщений. `MutationObserver` для автоматического развертывания плеера поверх тегов `.voice-msg` и поддержка перемотки жестами по волновой диаграмме.
* **Внутренние зависимости:** Нет. Инициализируется на событии `DOMContentLoaded`.

---

## 4. Описание модулей мессенджера (Папка `n0g_messenger_ui/`)

### 📄 n0g_msg_utils_101.js
* **Тип:** Чистый JS (ES6). Не содержит React-кода, JSX или обращений к DOM.
* **Ответственность:** Статические данные (заглушки контактов, темы, эмодзи) и чистые функции-хелперы (расчет сетки альбомов картинок, форматирование длительности аудио, расчет безопасных координат контекстного меню).

### 📄 n0g_msg_bridge_102.js
* **Тип:** Чистый JS (ES6). Не содержит React-кода.
* **Ответственность:** Безопасный интерфейс-фасад (адаптер) для обращения React-слоя к методам `bridge-core.js` и Kotlin. Предотвращает ошибки («вылеты») UI при отладке в обычном браузере на ПК.

### 📄 n0g_msg_settings_view_103.js
* **Тип:** React UI-модуль.
* **Ответственность:** Компоненты настроек (`SettingsPanel` — оформление, приватность никнейма, очистка нативного кэша) и тем оформления (`ThemePanel` — прозрачность и цвет баблов чата).

### 📄 n0g_msg_ui_modals_104.js
* **Тип:** React UI-модуль.
* **Ответственность:** Компоненты левой панели со списком чатов (`Sidebar`), карточки профиля (`ProfileModal`), полноэкранной галереи просмотра медиа (`Lightbox` с pinch-to-zoom жестами) и тостов (`Toast`).

### 📄 n0g_msg_chat_area_105.js
* **Тип:** React UI-модуль.
* **Ответственность:** Область переписки. Содержит рендеринг строки сообщения (`MessageRow`), контекстное всплывающее меню действий (`ContextMenu`) и центральную панель диалога (`ChatArea` — поле ввода, нативная запись звука, закрепленные сообщения).

### 📄 n0g_msg_app_entry_106.js
* **Тип:** Точка входа React (React-DOM монтирование).
* **Ответственность:** Инициализация мессенджера, хранение корневого React-стейта, монтирование дерева компонентов в DOM и реакция на системные события Котлина.
* **Экспортирует:** По умолчанию компонент `AppRoot`. Монтирует дерево в тег `<div id="root">`.

---

## 5. Совместимость с React-бандлом

Для работы скомпилированных React-вызовов (`T.useState`, `f.jsx`) напрямую в WebView без сборщиков, файлы настроек (103), модалок (104), чата (105) и точки входа (106) содержат в самом начале совместимый заголовок для маппинга глобальных переменных:

    const T = window.T || window.React;
    const Q0 = window.Q0 || window.ReactDOM || window.ReactDom; // только в app_entry_106
    const f = window.f || {
        jsx: (type, props, key) => T.createElement(type, { ...props, key }),
        jsxs: (type, props, key) => T.createElement(type, { ...props, key })
    };

Этот заголовок перенаправляет вызовы на глобальную оптимизированную библиотеку `react-lib.js`. При доработке UI-компонентов модифицировать или удалять этот заголовок запрещено.

---

## 6. Описание каскадных модулей стилей (Папка `n0g_messenger_ui/`)

Все стили мессенджера разделены на 6 модулей для изоляции зон ответственности и точечной кастомизации верстки без влияния на другие элементы.

### 📄 style-base.css (Базовые переменные и сброс)
* **Ответственность:** Шрифты, CSS-переменные системных цветов оформления, универсальный сброс браузерных стилей, кастомизация скроллбаров и базовая разметка контейнера `.messenger-app`.
* **Внутренние зависимости:** Нет.

### 📄 style-sidebar.css (Сайдбар и шапка чата)
* **Ответственность:** Базовая компоновка левой панели (Сайдбар со списком чатов) и шапка активного диалога диалогового окна.
* **Внутренние зависимости:** Требует предварительного подключения `style-base.css`.

### 📄 style-messages.css (Сообщения и поле ввода)
* **Ответственность:** Стили баблов переписки (входящие, исходящие, выделенные), закрепленного сообщения, превью ответов и формы ввода текста в нижней панели чата.
* **Внутренние зависимости:** Зависит от: `style-base.css`.

### 📄 style-modals.css (Модалки: контекстное меню, профиль, лайтбокс, настройки)
* **Ответственность:** Элементы интерфейса поверх основного слоя — контекстное меню действий, оверлеи настроек тем, окно просмотра информации о контакте и базовые стили лайтбокса.
* **Внутренние зависимости:** Требует предварительного подключения `style-base.css`.

### 📄 style-media.css (Медиа: альбомы, видео, адаптив, глитч, гласс)
* **Ответственность:** Адаптивные медиа-запросы (мобильные, планшетные и портретные брейкпоинты), разметка альбомов, усечение текста ответов, глитч-анимация логотипа nan0gram и глассморфизм стеклянных карточек чата.
* **Внутренние зависимости:** Зависит от: `style-base.css`, `style-sidebar.css`, `style-messages.css`.

### 📄 style-player.css (Аудиоплеер, запись голоса, файловые сообщения)
* **Ответственность:** Кастомный аудиоплеер голосовых сообщений, оверлей записи голоса с таймером, алая кнопка сброса записи, индикатор-замочек блокировки, файловые сообщения и подсветка кнопки прокрутки вниз.
* **Внутренние зависимости:** Зависит от: `style-base.css`, `style-messages.css`.

---

## 7. Описание корневых файлов (Папка `app/src/main/assets/`)

### 📄 cipher.js
* **Тип:** Чистый JS (ES6). Глобальный контекст.
* **Ответственность:** Криптографический модуль и системные утилиты. Предоставляет методы шифрования payloads через GCM AES и RSA (с использованием нативного Android-моста), а также обфускацию зашифрованных строк в псевдослова (chaos masking) для маскировки сетевых пакетов. Регистрирует глобальные объекты `window.nanoCipher` и `window.nanoUtils` (методы работы со стейтом сессии, localStorage, генерацией псевдослучайных ключей и энтропией заголовков).
* **Внутренние зависимости:** Нет.

### 📄 react-lib.js
* **Тип:** Оптимизированный JS-бандл. Глобальный контекст.
* **Ответственность:** Предоставляет ядро сред выполнения React и ReactDOM, а также предустановленный JSX-рантайм. Регистрирует на глобальном объекте `window` минифицированные псевдонимы `T` (React), `Q0` (ReactDOM) и `f` (JSX Runtime), которые используются всеми UI-модулями React-мессенджера.
* **Внутренние зависимости:** Нет.

### 📄 index.html
* **Тип:** Статический HTML5 файл.
* **Ответственность:** Декларативно выстраивает очередность загрузки каскадных таблиц стилей в теге `<head>` и упорядочивает подключение глобальной криптографии, моста Android, ядра React и ES-модуля фронтенда в теле `<body>`.
* **Внутренние зависимости:** Ссылается на каскадные стили в папке `n0g_messenger_ui/`, криптографический модуль `cipher.js`, библиотеки React в корне, скрипты нативного моста `n0g_bridge/` и ES-модуль `n0g_messenger_ui/n0g_msg_app_entry_106.js`.

---

## 8. Архитектура нативного слоя Android (Kotlin)

Нативный слой приложения разделен на специализированные компоненты ядра, обособленный пакет интерфейса пользователя и системные JS-интерфейсы для изоляции логики работы с WebView, системными ресурсами и криптографией [1].

### 📁 app/src/main/java/com/example/ (Корневые модули ядра)

* **`MainActivity.kt`** — Главная Activity приложения на Jetpack Compose. Отвечает за проверку отладочных StrictMode-правил, определение Secure Android ID устройства, запуск системных разрешений микрофона, инициализацию синглтона БД, запуск фоновых циклов инспекции DOM-окружения и рендеринг контейнера `AppScreen`.
* **`AppDatabase.kt`** — Нативная база данных Room. Содержит RoomDatabase-синглтон `AppDatabase`, сущности таблиц `ChatEntity` и `MessageEntity` с индексами быстродействия по штампу времени, и репозиторий `NanogramRepository` (выполняющий безопасные обернутые транзакции в контексте `Dispatchers.IO` с обратным выводом в нативный логгер).
* **`BridgeEngine.kt`** — Низкоуровневый движок симуляции пользовательского ввода. Содержит функцию `simulateTouch` для симуляции физических тапов по WebView с автоматическим масштабированием на плотность пикселей экрана устройства, функцию `simulateType` для незаметного наполнения DOM-полей ввода без открытия экранной клавиатуры IME, и `BridgeEffects` для запуска фоновых циклов сканирования верстки.
* **`BridgeScripts.kt`** — Каталог инжектируемых JS-скриптов и CSS-селекторов. Содержит строковые JS-программы для непрерывного мониторинга сессии, сканирования физических координат скрепки на странице почты, временного блокирования фокуса ввода, заполнения полей получателя/темы и Радар-скрипт фонового парсинга новых входящих писем.
* **`MediaManager.kt`** — Нативный менеджер файлового кэша. Создает изолированную директорию `nan0gram_media`, переводит Base64-содержимое из JS-моста во временные файлы, копирует выбранные документы из Uri контент-провайдеров, рассчитывает текущий объем занятой памяти для UI Настроек и предоставляет физический путь для WebView-ассетов.
* **`UpdateChecker.kt`** — Модуль взаимодействия с удаленным сервером обновлений. Проверяет новые APK-релизы через GitHub API по тегам `build-*`. Реализует OTA-механизм обновлений: сравнивает SHA/тег текущей веб-версии с удаленным архивом, выкачивает `web_update.zip`, производит безопасную распаковку без риска Zip Slip уязвимостей в папку ассетов и выдает нативный Toast о необходимости перезапуска.
* **`UpdateDialog.kt`** — Графический интерфейс инсталлятора. Легковесный Composable-диалог `AlertDialog`, запускаемый при обнаружении свежей сборки приложения, отображающий текст изменений и перенаправляющий пользователя на URL скачивания APK.

### 📁 app/src/main/java/com/example/bridge/ (Модули системного JS-интерфейса)

* **`UkrnetJsInterface.kt`** — Системный интерфейс управления скрытой почтой Ukr.net. Отвечает за логирование из JS, перехват событий успешной авторизации, координацию готовности формы и отправку входящих писем в мессенджер.
* **`MessengerJsInterface.kt`** — Облегченный JS-мост React-мессенджера. Служит прокси-контроллером с аннотированными `@JavascriptInterface` методами, делегируя тяжелые вычислительные задачи специализированным ленивым хелперам (`by lazy`).
* **`MessengerCryptoHelper.kt`** — Вспомогательный криптографический класс. Отвечает за нативную реализацию алгоритмов асимметричного шифрования RSA и симметричного блочного шифрования AES-GCM-256 для защиты трафика.
* **`MessengerDownloadHelper.kt`** — Вспомогательный класс загрузки медиафайлов. Производит декодирование входящих Base64-данных, сопоставление MIME-типов, сохранение байтов в системную папку Downloads (с поддержкой Scoped Storage на Android Q+) и вывод тостов успеха через JS.
* **`MessengerComposeHelper.kt`** — Вспомогательный класс управления формой создания письма. Обеспечивает отслеживание таймингов повторной отправки (Recoil), симуляцию тапов на скрытые кнопки вложений и автоматическое завершение отправки письма по событию завершения загрузки файлов.
* **`MessengerDbHelper.kt`** — Вспомогательный класс взаимодействия с базой данных. Выполняет асинхронные операции сохранения истории переписок, удаления сообщений, обновления реакций и очистки кэша из фонового потока корутин.
* **`MessengerAudioHelper.kt`** — Вспомогательный класс управления аудиофокусом. Отвечает за монопольный захват нативного аудиофокуса ОС Android на время записи голосовых сообщений и его последующее корректное освобождение.

### 📁 app/src/main/java/com/example/ui/ (Модули графического интерфейса)

* **`AppScreen.kt`** — Основной Compose-экран приложения. Выполняет предварительную привязку БД и файловых директорий к JS-интерфейсу перед рендерингом, а также выстраивает базовый каркас (`Scaffold`) для размещения слоев UI.
* **`WebViewLayer.kt`** — Управляющий контейнер наложения. Хранит ссылки на инстансы WebViews, координирует системные диалоговые окна выбора файлов Android и перенаправляет корутины шифрования во временные бинарные файлы.
* **`UkrnetWebView.kt`** — Конструктор скрытого WebView почты Ukr.net. Отвечает за загрузку шлюза отправки писем, инжекцию JS-скриптов заполнения полей и интеграцию локально записанных голосовых сообщений.
* **`MessengerWebView.kt`** — Конструктор WebView мессенджера. Отвечает за прозрачный фон, очистку Chromium-кэша при запуске для корректной установки OTA-обновлений, перехват JS-логов в консоль отладки и инжекцию патчей перехвата кликов на кнопки аттачей.
* **`MediaLimitsProcessor.kt`** — Предохранитель сетевого шлюза. Проверяет форматы и размеры файлов перед шифрованием и выводит предупреждения в JS при выходе за пределы лимитов Ukr.net (максимум 1 произвольный файл до 17 МБ, либо до 8 фото/2 видео до 15 МБ суммарно).
* **`MediaUtils.kt`** — Утилиты прямого доступа к файловой системе ОС. Осуществляют считывание размеров по нативным `Uri`, кэширование временных копий, симметричное шифрование алгоритмом AES-GCM-256 по JS-ключам и преобразование картинок/превью видео в Base64.
* **`WebViewHelpers.kt`** — Логика навигационных барьеров. Содержит extension-функции для принудительного возврата фонового WebView на целевой URL создания сообщения (`sendmsg`) при попытках почтового шлюза перенаправить сессию.
* **`FallbackAssetsPathHandler.kt`** — Роутер OTA-ресурсов. Инкапсулирует логику приоритетного чтения статических файлов (CSS/JS) из папки локального диска для поддержки «горячих» обновлений без пересборки всего APK.
* **`LogPanel.kt`** — Панель отладки. Компактный плавающий компонент, выводящий отфильтрованные консольные логи WebView в реальном времени, а также предоставляющий ползунок для плавной регулировки прозрачности интерфейса мессенджера.
